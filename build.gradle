plugins {
    id 'java'
    id 'application'
}

version = '1.0.1'

sourceSets {
  main {
    resources {
        srcDirs = ["src/main/java"]
        includes = ["**/*.fxml"]
    }
  }
}

mainClassName = 'app.MainApp'

jar {
    manifest {
        attributes 'Main-Class': 'app.MainApp'
    }
}
